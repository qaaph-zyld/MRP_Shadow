-- Canonical wrapper: Diamond / Schedule Variance (stub, 2798 only)
-- Source: MRP/Diamond_Schedule_Variance_stub.sql
-- NOTE: Uses supplier release history table (sa_release_hist). Adjust when structure is confirmed.
:r ..\..\MRP\Diamond_Schedule_Variance_stub.sql
